# Requirements for standalone mode with Physics v2.0 (Python 3.11+ compatible)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9

# Physics engine dependencies
numpy>=2.0.0
scipy>=1.14.0

# Optional: For CDSE data access (not required for demo)
httpx>=0.27.0
