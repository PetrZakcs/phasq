version: '3.8'

services:
  # ===== FastAPI Backend =====
  backend:
    build: ./backend
    container_name: phasq-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=postgresql://phas:password@db:5432/phasq
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - DEBUG=true
    depends_on:
      - db
      - redis
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - phasq-network

  # ===== Celery Worker =====
  celery-worker:
    build: ./backend
    container_name: phasq-celery-worker
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=postgresql://phas:password@db:5432/phasq
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    depends_on:
      - db
      - redis
    command: celery -A app.workers.celery_app worker --loglevel=info
    networks:
      - phasq-network

  # ===== PostgreSQL + PostGIS Database =====
  db:
    image: postgis/postgis:15-3.3
    container_name: phasq-db
    environment:
      - POSTGRES_USER=phas
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=phasq
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - phasq-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U phas -d phasq" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===== Redis =====
  redis:
    image: redis:7-alpine
    container_name: phasq-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - phasq-network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:


networks:
  phasq-network:
    driver: bridge
